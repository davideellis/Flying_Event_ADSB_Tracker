Metadata-Version: 2.4
Name: flying-event-adsb-tracker
Version: 0.1.0
Summary: Low-cost event aircraft tracking for ride events
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: fastapi<1.0,>=0.115
Requires-Dist: uvicorn[standard]<1.0,>=0.30
Requires-Dist: sqlalchemy<3.0,>=2.0
Requires-Dist: alembic<2.0,>=1.13
Requires-Dist: jinja2<4.0,>=3.1
Requires-Dist: python-multipart<1.0,>=0.0.9
Requires-Dist: itsdangerous<3.0,>=2.2
Requires-Dist: email-validator<3.0,>=2.2
Requires-Dist: passlib<2.0,>=1.7
Requires-Dist: pydantic-settings<3.0,>=2.3
Requires-Dist: httpx<1.0,>=0.27
Requires-Dist: psycopg[binary]<4.0,>=3.2
Provides-Extra: dev
Requires-Dist: pytest<9.0,>=8.2; extra == "dev"
Requires-Dist: pytest-cov<6.0,>=5.0; extra == "dev"
Requires-Dist: pytest-asyncio<1.0,>=0.23; extra == "dev"
Requires-Dist: ruff<1.0,>=0.5; extra == "dev"

# Flying Event ADS-B Tracker

A low-cost web application for managing and viewing aircraft ride events such as Young Eagles and Challenge Air. The system lets admins configure events, track specific aircraft by tail number, manage passenger assignment queues, and expose a public spectator map showing aircraft state and movement.

## Status

The repo now includes the first working implementation slice:
- FastAPI application with server-rendered public and admin pages
- SQLAlchemy models for users, events, aircraft, passengers, tracks, and state history
- Core domain logic for passenger queues, track archiving, and automatic `Flying`/`Arrived` transitions
- Initial Alembic migration
- Unit and integration tests covering the core domain and app flows
- Terraform baseline for a low-cost AWS Lightsail deployment

## Chosen Stack

- Backend: FastAPI
- Database layer: SQLAlchemy 2.x + Alembic
- Templates/UI: Jinja2 + vanilla JavaScript + Leaflet
- Password hashing: `pbkdf2_sha256` via Passlib
- Production target: PostgreSQL on a single AWS Lightsail instance
- Local default: SQLite for quick setup
- Infra: Terraform in [`terraform/`](./terraform)

This stack was chosen to keep deployment simple on one VM while still giving us good testing support and a clean upgrade path later.

## Current Features

### Public
- Published event list
- Direct event page by slug
- Public aircraft states
- Map with current and archived tracks
- JSON endpoint for event/map polling
- Passenger names hidden from public responses

### Admin
- Email/password login
- Password reset token flow for local/dev use
- Bootstrap admin user on first startup
- Create events
- Add tracked aircraft
- Manually set aircraft state
- Queue passengers
- Activate the next passenger and archive the prior track
- Create additional admin users

### Domain automation
- Transition to `Flying` automatically when a tracked aircraft is observed airborne inside the event area
- Transition to `Arrived` automatically when the aircraft stays inside the configured return radius for the configured hold time
- Manual overrides still supported

## Repo Layout

- `src/app/`: FastAPI app code
- `migrations/`: Alembic migration files
- `tests/`: Unit and integration tests
- `docs/architecture.md`: Architecture direction and hosting rationale
- `docs/database-schema.md`: Data model and domain rules
- `docs/feature-plan.md`: Delivery plan and testing guidance
- `terraform/`: Terraform for AWS infrastructure

## Local Setup

### Requirements
- Python 3.11+

### Install
```bash
python -m pip install -e .[dev]
```

### Configure
```bash
copy .env.example .env
```

Important settings:
- `FEAT_SECRET_KEY`
- `FEAT_DATABASE_URL`
- `FEAT_BOOTSTRAP_ADMIN_EMAIL`
- `FEAT_BOOTSTRAP_ADMIN_PASSWORD`
- `FEAT_PASSWORD_RESET_BASE_URL`

### Run the app
```bash
python -m uvicorn app.main:app --reload
```

Open:
- Public home: `http://localhost:8000/`
- Admin login: `http://localhost:8000/auth/login`

Default bootstrap admin comes from `.env` or the built-in defaults.

## Testing

Run tests:
```bash
python -m pytest
```

Run linting:
```bash
python -m ruff check .
```

Current automated coverage includes:
- geofence math
- state transitions
- passenger queue activation and track archival
- public payload redaction
- admin login and basic event/passenger workflows

## Database Notes

The app currently uses SQLAlchemy models and an initial Alembic migration.

For local development, SQLite is the default.
For production on Lightsail, we should use PostgreSQL and point `FEAT_DATABASE_URL` at it.

## Terraform

Terraform stays isolated under `terraform/`.

Current baseline includes:
- Lightsail instance
- Static IP
- Public ports for SSH/HTTP/HTTPS
- Optional SSH key registration

Before applying Terraform, review bundle size and region to make sure the monthly cost stays within the target budget.

## README Maintenance Rule

Keep this README aligned with implementation changes. If the architecture, setup, stack, or major behavior changes, update the README in the same change whenever practical.

## Next Recommended Build Steps

1. Add full event editing, publish/unpublish, and active/inactive controls in the admin UI
2. Add real ADS-B provider ingestion behind a provider interface and a background poller
3. Add PostgreSQL-specific local/dev compose setup and production deployment scripts
4. Expand integration tests around auth, event editing, and automated arrival detection
